using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Goal : PathEntity
{
    // Start is called before the first frame update
    void Start()
    {
        RandomPosition();
    }
}
